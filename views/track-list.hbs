<h2>Album {{this.albumTitle}}</h2>
{{#each results}}
    <h3>{{this.name}}</h3>
    <audio
        controls
        src="{{this.preview_url}}">
            <a href="{{this.preview_url}}">
                Download audio
            </a>
    </audio>
{{/each}}